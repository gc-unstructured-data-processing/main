# 03_mining v1 — Spearman·OLS·specificity 보고서

> **작성자**: 이동원 · **작성일**: 2026-05-18 · **사이클**: 1·2 fix 적용
> **노트북**: `notebooks/03_mining/v1/03_mining.ipynb` (15 cells)
> **상태**: Samsung 1건 N=1 → 모든 통계 NaN (의도된 형식 검증). 사이클 3 batch 후 정상 통계.

---

## 1. 데이터

### 1.1 입력
- `data/processed/esg_features_v2.parquet` (02 v2 산출, Samsung 1행)
- `logs/kcgs_lineage.csv` (00 ingest 산출, 381 행 — 등급 join용)
- `data/interim/005930_2024_sections.json` (specificity score 계산용)

### 1.2 출력
- `data/processed/regression_results_v1.csv` — placeholder (N=1이라 NaN, 사이클 3 후 정상 채워짐)
- `data/processed/fig_v1_mining_cheaptalk.png` — word_count + specificity bar (Fix A 적용)

---

## 2. 방법

### 2.1 Step 매핑 (가이드 03 line 159-274 + 04 §3 평가표)
| Step | What | 가이드 |
|---|---|---|
| Safety | Fix A 폰트 + Fix E pandas display | — |
| 1 | esg_features_v2 + kcgs_lineage join + N 확인 | — |
| 2 | feature_validity 함수 (Spearman + t-test + Mann-Whitney) — 9 (feature, grade) 쌍 | 03 line 159-197 |
| 3 | OLS baseline + word_count cheap-talk 통제 | 03 line 230-237 |
| 4 | 모형 선택 근거 표 (OLS·ordered_logit·binary_logit 비교) | 03 line 220-274 |
| 5 | alpha — specificity score (벤치마킹 4번 ClimateBert) | 04 §3-1 |
| 6 | self-check + 사이클 3 후 재실행 안내 | 04 §2-1 |

### 2.2 Spearman·t-test 함수 (feature_validity)
```python
def feature_validity(features_df, score_col, grade_col):
    df = features_df.dropna(subset=[score_col, grade_col]).copy()
    n = len(df)
    if n < 3:
        return NaN dict  # N=1·N=2는 NaN 반환
    rho, p = stats.spearmanr(df[score_col], df[grade_col])
    med = df[grade_col].median()
    high = df[df[grade_col] >= med][score_col]
    low = df[df[grade_col] < med][score_col]
    if len(high) < 2 or len(low) < 2:
        t_stat = t_p = NaN
    else:
        t_stat, t_p = stats.ttest_ind(high, low, equal_var=False)
    ...
```

### 2.3 9 (feature, grade) 쌍
| feature | grade |
|---|---|
| seed_tfidf_E | esg_grade_num |
| seed_tfidf_S | esg_grade_num |
| seed_tfidf_G | esg_grade_num |
| cosine_E | esg_grade_num |
| cosine_S | esg_grade_num |
| cosine_G | esg_grade_num |
| seed_tfidf_E | e_grade_num (차원 정합성) |
| seed_tfidf_S | s_grade_num |
| seed_tfidf_G | g_grade_num |

### 2.4 OLS baseline (가이드 03 line 230-237)
`esg_grade_num = β0 + β1·seed_tfidf_E + β2·word_count + ε`
- β1 의미: 텍스트 점수 단위 변화당 등급 변화
- β2 word_count: cheap-talk 통제

### 2.5 specificity score (alpha)
- 정의: (숫자 포함 토큰) / 총 토큰
- ClimateBert 라인 (벤치마킹 4번): cheap-talk 강화 통제

---

## 3. 결과 (N=1, 의도된 형식 검증)

### 3.1 feature_validity 표 (9 쌍 모두 NaN)
| feature | grade | n | Spearman ρ | p | t_stat | t_p |
|---|---|---:|---:|---:|---:|---:|
| seed_tfidf_E | esg_grade_num | 1 | NaN | NaN | NaN | NaN |
| seed_tfidf_S | esg_grade_num | 1 | NaN | NaN | NaN | NaN |
| seed_tfidf_G | esg_grade_num | 1 | NaN | NaN | NaN | NaN |
| ... (9 rows 모두 NaN) | | | | | | |

**보고서 형식**:
> `seed_tfidf_E ↔ esg_grade_num | ρ=nan | p=nan | ⚠️ N 부족 — v3 batch (N≥30) 후 재실행 필요`

### 3.2 OLS (적합 불가)
- N=1·k=3 → df_residual = -2 → 적합 자체 불가
- 노트북에서는 "v3 batch (N≥30) 후 동일 셀 재실행 → coef + std_err 정상 산출" 안내 출력

### 3.3 specificity score (Samsung 2024)
- word_count: 12,159
- specificity_score: 계산 가능 (단일 firm-year 값)

### 3.4 모형 선택 근거 표
| 모형 | 장점 | 한계 | 본 v1 선택 |
|---|---|---|---|
| OLS | 직관적 baseline | 등급 ordinal에 등간성 가정 | ⭐ 채택 |
| Ordered Logit | ordinal 적합 | 계수 해석 어려움, 표본 작으면 불안정 | ➡️ v2 batch 후 |
| Binary Logit (A 이상) | odds 해석 쉬움 | 등급 미세 차이 손실 | ➡️ v2 batch 후 |

---

## 4. 한계

### 4.1 N=1 (가장 큰 한계)
- 모든 통계 추정 NaN — scipy.stats 표준 동작 (N≥3·자유도 부족)
- **사이클 3 batch 30건이 즉시 해결** (코드 변경 0줄)

### 4.2 OLS baseline만 (사이클 3 v1)
- ordered_logit·binary_logit 코드 build 안 됨. 사이클 4 v2에서 추가
- 가이드 03 line 220-274 3 옵션 모두 비교 의무

### 4.3 cheap-talk 단순화
- 본 v1은 word_count + specificity 2종
- ClimateBert (벤치마킹 4번) 라인: commitment·non-material 추가 권장 (알파)

### 4.4 alpha 1개만 (specificity)
- 가이드 04 §3-1: 알파 1개 이상 권장
- 사이클 4에서 업종별 분해·section_weighted score·KLUE-RoBERTa 중 1개 추가

### 4.5 인과 vs 연관
- 본 v1 결과는 모두 NaN이라 해석 단언 X
- 사이클 3 후에도 가이드 04 line 99-111 "인과 ❌ 연관 ⭕" 유지

---

## 5. 위반금지 8항목

| # | 위반금지 | 본 v1 |
|---|---|---|
| 1 | 가짜 0 ❌ | ✅ N=1 시 NaN 반환 (가짜 ρ=0 안 함) |
| 3 | MCP 그대로 신뢰 ❌ | ✅ Spearman·t-test 함수 직접 구현 |
| 6 | 진단 부재 ❌ | ✅ N=1 안내 + 사이클 3 후 재실행 가이드 |
| 8 | 평가 기준 부재 ❌ | ✅ ρ ≥ 0.10 & p < 0.05 → 약한 연관성 명시 |

---

## 6. 다음 단계 연결

- **사이클 3 phase2B 후**: 본 노트북 재실행 → 9 NaN → 정상 ρ·p·t_stat·OLS β
- **사이클 4 v2**: ordered_logit + binary_logit + 알파 1개 (업종별 분해 권장)

---

> **본 v1은 풀 사이클 v1의 마지막 단계. 코드·표·시각화 형식 모두 완성. 통계 추정은 사이클 3 batch 1회로 자동 충족.**
